# Порядок запуска проекта

Откройте консоль. перейдите в папку с проектом, введите следующую команду:
```bash
docker-compose up --build -d --force-recreate
```

Далее перейдите в swagger:
```
http://127.0.0.1:8000/docs
```